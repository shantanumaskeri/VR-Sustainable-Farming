namespace SpatialStories
{
    public enum S_ReloadSubject
    {
        NONE, // no reload
        CONDITIONS, // conditions only will be reloaded
        CONDITIONS_AND_DEPENDENCIES // conditions and dependencies will be reloaded
    }
}